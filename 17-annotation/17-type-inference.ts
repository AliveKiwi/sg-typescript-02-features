// Hover over the variables to see their inferred types
// TypeScript will infer the types based on the assigned values

let apples = 5;
let speed = 'fast';
let hasName = true;
let nothingMuch = null;
let nothing = undefined;

// built in objects
let now = new Date();
